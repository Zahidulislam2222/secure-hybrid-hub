# Secure Hybrid Hub rules

Use the broker CLI and its typed results. Do not bypass path, network, provider,
credential, state, checkpoint, or approval controls. Never claim verification
from model confidence. During the authorized local-only slice, cloud egress,
internet research, secret use, production access, deployment, downloads, and
installations remain disabled.
