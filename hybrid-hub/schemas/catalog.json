{
  "schema_version": "1.0.0",
  "artifacts": {
    "system-profile": {"version": "1.0.0", "required_payload": ["name", "profiles", "roots"]},
    "project-dossier": {"version": "1.0.0", "required_payload": ["purpose", "hierarchy", "provenance"]},
    "dossier-change-proposal": {"version": "1.0.0", "required_payload": ["changes", "rationale"]},
    "dossier-safe-projection": {"version": "1.0.0", "required_payload": ["facts", "excluded"]},
    "repository": {"version": "1.0.0", "required_payload": ["repo_id", "path"]},
    "component": {"version": "1.0.0", "required_payload": ["component_id", "repository_id"]},
    "dependency-edge": {"version": "1.0.0", "required_payload": ["from", "to", "kind"]},
    "environment": {"version": "1.0.0", "required_payload": ["name", "classification"]},
    "provider-profile": {"version": "1.0.0", "required_payload": ["provider", "mode", "endpoint"]},
    "policy-decision": {"version": "1.0.0", "required_payload": ["decision", "reason"]},
    "task": {"version": "1.0.0", "required_payload": ["request", "state"]},
    "classification": {"version": "1.0.0", "required_payload": ["class", "reasons"]},
    "plan-request": {"version": "1.0.0", "required_payload": ["outcome", "constraints"]},
    "plan": {"version": "1.0.0", "required_payload": ["steps", "acceptance_criteria"]},
    "research-request": {"version": "1.0.0", "required_payload": ["question", "preferred_domains"]},
    "evidence": {"version": "1.0.0", "required_payload": ["source", "retrieved_at", "hash"]},
    "implementation-request": {"version": "1.0.0", "required_payload": ["plan", "scope"]},
    "implementation-result": {"version": "1.0.0", "required_payload": ["status", "changed_paths"]},
    "test-command": {"version": "1.0.0", "required_payload": ["command_id", "arguments"]},
    "test-result": {"version": "1.0.0", "required_payload": ["command_id", "exit_code"]},
    "quality-summary": {"version": "1.0.0", "required_payload": ["passed", "gates"]},
    "egress-manifest": {"version": "1.0.0", "required_payload": ["provider", "files", "bundle_hash"]},
    "review-request": {"version": "1.0.0", "required_payload": ["bundle_hash", "questions"]},
    "review": {"version": "1.0.0", "required_payload": ["findings", "decision"]},
    "repair-request": {"version": "1.0.0", "required_payload": ["failure_hash", "attempt"]},
    "release-manifest": {"version": "1.0.0", "required_payload": ["repositories", "deployment_order"]},
    "deployment-plan": {"version": "1.0.0", "required_payload": ["environment", "artifact_ids"]},
    "deployment-result": {"version": "1.0.0", "required_payload": ["environment", "status"]},
    "approval": {"version": "1.0.0", "required_payload": ["subject", "approver", "decision"]},
    "audit-event": {"version": "1.0.0", "required_payload": ["event_type", "event_hash"]},
    "final-status": {"version": "1.0.0", "required_payload": ["status", "evidence"]},
    "project-modifier": {"version": "1.0.0", "required_payload": ["name", "classification_floor", "deny_actions"]},
    "guided-plan": {"version": "1.0.0", "required_payload": ["outcome", "packets", "acceptance_criteria"]},
    "guided-work-packet": {"version": "1.0.0", "required_payload": ["packet_id", "objective", "repository_ids", "allowed_paths", "deliverables", "research", "research_required", "research_guidance"]},
    "research-evidence-packet": {"version": "1.0.0", "required_payload": ["evidence_packet_hash", "items", "degraded_reasons"]}
  }
}
