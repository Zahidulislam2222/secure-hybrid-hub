Use only the broker's explicit promotion command. Never run a cloud CLI directly,
handle production credentials, or bypass staging, canary, health, human approval,
and rollback gates. Arguments: $ARGUMENTS
