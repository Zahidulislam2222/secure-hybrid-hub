Register this project only through the Secure Hybrid AI Development Hub CLI. Use
`hub.py system init`, then deterministic discovery and dossier review. Never
approve on the user's behalf or read secrets, environment values, sibling paths,
or production data. Arguments: $ARGUMENTS
