Read the broker pause reason, confirm the exact supplied authorization, and call
`hub.py resume` only to the allowed state. General “continue” never grants new
filesystem, network, cloud, credential, or production authority. Arguments:
$ARGUMENTS
