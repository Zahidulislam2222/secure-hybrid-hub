---
name: hybrid-cancel
description: Cancel a broker task and preserve its recoverable workspace and evidence.
---

Call `python3 hybrid-hub/hub.py cancel` with `$ARGUMENTS` and report the exact
confirmed state.
