---
name: hybrid-init
description: Register and discover a project topology through the Secure Hybrid AI Development Hub.
---

Call `python3 hybrid-hub/hub.py system init` with `$ARGUMENTS`. Present the
discovery and draft dossier. Never approve for the user or read secrets,
environment values, production data, or sibling paths.
